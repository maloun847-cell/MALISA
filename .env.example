# Secret used to sign the bidder "paddle" cookie. Required in production.
MALISA_SECRET=change-me-to-a-long-random-string

# Where the JSON store and uploaded photos live (defaults to ./.data).
# MALISA_DATA_DIR=/var/lib/malisa
